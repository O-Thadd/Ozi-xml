<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        android:padding="8dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/avi_imageView"
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:src="@drawable/male_profile_black"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:src="@drawable/male_profile_black" />

            <TextView
                android:id="@+id/chatMateUsername_textView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="sans-serif-black"
                android:textSize="16sp"
                app:layout_constraintStart_toEndOf="@id/avi_imageView"
                app:layout_constraintTop_toTopOf="@id/avi_imageView"
                app:layout_constraintBottom_toBottomOf="@id/avi_imageView"
                tools:text="O.Thadd" />

            <ImageView
                android:id="@+id/verificationStatusIndicator_imageView"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_verified"
                android:layout_marginStart="4dp"
                app:layout_constraintStart_toEndOf="@id/chatMateUsername_textView"
                app:layout_constraintTop_toTopOf="@id/avi_imageView"
                app:layout_constraintBottom_toBottomOf="@id/avi_imageView"/>

            <ImageView
                android:id="@+id/onlineIndicator_imageView"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:src="@drawable/ic_online"
                android:layout_marginEnd="8dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@id/avi_imageView"
                app:layout_constraintBottom_toBottomOf="@id/avi_imageView"
                app:tint="@color/ozi_green" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </RelativeLayout>

</layout>